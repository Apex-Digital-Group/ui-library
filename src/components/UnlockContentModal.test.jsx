import { describe, it, expect } from 'vitest'
import UnlockContentModal from './UnlockContentModal'

describe('UnlockContentModal', () => {
  it('exports UnlockContentModal', () => {
    expect(UnlockContentModal).toBeDefined()
  })
})
