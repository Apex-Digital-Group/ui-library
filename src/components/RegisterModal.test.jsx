import { describe, it, expect } from 'vitest'
import RegisterModal from './RegisterModal'

describe('RegisterModal', () => {
  it('exports RegisterModal', () => {
    expect(RegisterModal).toBeDefined()
  })
})
