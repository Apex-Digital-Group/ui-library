import { describe, it, expect } from 'vitest'
import ReferTierProgress from './ReferTierProgress'

describe('ReferTierProgress', () => {
  it('exports ReferTierProgress', () => {
    expect(ReferTierProgress).toBeDefined()
  })
})
