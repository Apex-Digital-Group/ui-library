import { describe, it, expect } from 'vitest'
import ReferFounderDiscount from './ReferFounderDiscount'

describe('ReferFounderDiscount', () => {
  it('exports ReferFounderDiscount', () => {
    expect(ReferFounderDiscount).toBeDefined()
  })
})
