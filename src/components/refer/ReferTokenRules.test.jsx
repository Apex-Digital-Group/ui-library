import { describe, it, expect } from 'vitest'
import ReferTokenRules from './ReferTokenRules'

describe('ReferTokenRules', () => {
  it('exports ReferTokenRules', () => {
    expect(ReferTokenRules).toBeDefined()
  })
})
