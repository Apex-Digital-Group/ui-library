import { describe, it, expect } from 'vitest'
import ReferRewardWallet from './ReferRewardWallet'

describe('ReferRewardWallet', () => {
  it('exports ReferRewardWallet', () => {
    expect(ReferRewardWallet).toBeDefined()
  })
})
