import { describe, it, expect } from 'vitest'
import ReferTokenWallet from './ReferTokenWallet'

describe('ReferTokenWallet', () => {
  it('exports ReferTokenWallet', () => {
    expect(ReferTokenWallet).toBeDefined()
  })
})
