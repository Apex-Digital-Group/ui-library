import { describe, it, expect } from 'vitest'
import ReferLinkCard from './ReferLinkCard'

describe('ReferLinkCard', () => {
  it('exports ReferLinkCard', () => {
    expect(ReferLinkCard).toBeDefined()
  })
})
