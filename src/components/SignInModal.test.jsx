import { describe, it, expect } from 'vitest'
import SignInModal from './SignInModal'

describe('SignInModal', () => {
  it('exports SignInModal', () => {
    expect(SignInModal).toBeDefined()
  })
})
