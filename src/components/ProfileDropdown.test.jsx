import { describe, it, expect } from 'vitest'
import ProfileDropdown from './ProfileDropdown'

describe('ProfileDropdown', () => {
  it('exports ProfileDropdown', () => {
    expect(ProfileDropdown).toBeDefined()
  })
})
