import { describe, it, expect } from 'vitest'
import TransactionsModal from './TransactionsModal'

describe('TransactionsModal', () => {
  it('exports TransactionsModal', () => {
    expect(TransactionsModal).toBeDefined()
  })
})
