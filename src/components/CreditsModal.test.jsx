import { describe, it, expect } from 'vitest'
import CreditsModal from './CreditsModal'

describe('CreditsModal', () => {
  it('exports CreditsModal', () => {
    expect(CreditsModal).toBeDefined()
  })
})
