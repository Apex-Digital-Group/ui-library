import { describe, it, expect } from 'vitest'
import OverviewScreen from './OverviewScreen'

describe('OverviewScreen', () => {
  it('exports OverviewScreen', () => {
    expect(OverviewScreen).toBeDefined()
  })
})
