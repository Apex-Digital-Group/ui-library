import { describe, it, expect } from 'vitest'
import PromotionPanel from './PromotionPanel'

describe('PromotionPanel', () => {
  it('exports PromotionPanel', () => {
    expect(PromotionPanel).toBeDefined()
  })
})
