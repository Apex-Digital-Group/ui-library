import { describe, it, expect } from 'vitest'
import PayoutModal from './PayoutModal'

describe('PayoutModal', () => {
  it('exports PayoutModal', () => {
    expect(PayoutModal).toBeDefined()
  })
})
