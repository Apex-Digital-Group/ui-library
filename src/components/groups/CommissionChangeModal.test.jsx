import { describe, it, expect } from 'vitest'
import CommissionChangeModal from './CommissionChangeModal'

describe('CommissionChangeModal', () => {
  it('exports CommissionChangeModal', () => {
    expect(CommissionChangeModal).toBeDefined()
  })
})
