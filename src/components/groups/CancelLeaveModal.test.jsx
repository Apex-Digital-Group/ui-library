import { describe, it, expect } from 'vitest'
import CancelLeaveModal from './CancelLeaveModal'

describe('CancelLeaveModal', () => {
  it('exports CancelLeaveModal', () => {
    expect(CancelLeaveModal).toBeDefined()
  })
})
