import { describe, it, expect } from 'vitest'
import GroupNotifications from './GroupNotifications'

describe('GroupNotifications', () => {
  it('exports GroupNotifications', () => {
    expect(GroupNotifications).toBeDefined()
  })
})
