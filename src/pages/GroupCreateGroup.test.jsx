import { describe, it, expect } from 'vitest'
import GroupCreateGroup from './GroupCreateGroup'

describe('GroupCreateGroup', () => {
  it('exports GroupCreateGroup', () => {
    expect(GroupCreateGroup).toBeDefined()
  })
})
