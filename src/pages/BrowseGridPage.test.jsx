import { describe, it, expect } from 'vitest'
import BrowseGridPage from './BrowseGridPage'

describe('BrowseGridPage', () => {
  it('exports BrowseGridPage', () => {
    expect(BrowseGridPage).toBeDefined()
  })
})
