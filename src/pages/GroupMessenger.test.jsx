import { describe, it, expect } from 'vitest'
import GroupMessenger from './GroupMessenger'

describe('GroupMessenger', () => {
  it('exports GroupMessenger', () => {
    expect(GroupMessenger).toBeDefined()
  })
})
