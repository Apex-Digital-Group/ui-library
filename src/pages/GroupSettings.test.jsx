import { describe, it, expect } from 'vitest'
import GroupSettings from './GroupSettings'

describe('GroupSettings', () => {
  it('exports GroupSettings', () => {
    expect(GroupSettings).toBeDefined()
  })
})
