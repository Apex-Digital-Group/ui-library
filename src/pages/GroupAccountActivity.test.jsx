import { describe, it, expect } from 'vitest'
import GroupAccountActivity from './GroupAccountActivity'

describe('GroupAccountActivity', () => {
  it('exports GroupAccountActivity', () => {
    expect(GroupAccountActivity).toBeDefined()
  })
})
