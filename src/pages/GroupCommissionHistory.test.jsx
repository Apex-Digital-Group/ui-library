import { describe, it, expect } from 'vitest'
import GroupCommissionHistory from './GroupCommissionHistory'

describe('GroupCommissionHistory', () => {
  it('exports GroupCommissionHistory', () => {
    expect(GroupCommissionHistory).toBeDefined()
  })
})
